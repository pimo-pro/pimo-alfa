# SPA: pasta f�sica public/industrial/release/ nao pode bloquear a rota /industrial
Options -Indexes
RewriteEngine On
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]
RewriteRule ^ /index.html [L]
