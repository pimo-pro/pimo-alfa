/**
 * Testes do motor de exportação (core/export, core/pdf).
 * Estrutura preparada para implementação futura.
 */

import { describe, it, expect } from "vitest";

describe("export", () => {
  it("placeholder: export engine", () => {
    expect(true).toBe(true);
  });
});
