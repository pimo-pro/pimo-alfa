/**
 * Testes do módulo de regras (core/rules).
 * Estrutura preparada para implementação futura.
 */

import { describe, it, expect } from "vitest";

describe("rules", () => {
  it("placeholder: módulo de regras", () => {
    expect(true).toBe(true);
  });
});
