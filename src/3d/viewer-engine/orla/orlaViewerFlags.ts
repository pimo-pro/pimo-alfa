/**
 * Flag de renderização 3D de ORLA.
 * Industrial (cálculo, PDF, custos, presets Admin) permanece 100% ativo.
 * Quando false: nenhum mesh/overlay de orla no Viewer.
 */
export const ORLA_VIEWER_RENDERING_ENABLED = false;
