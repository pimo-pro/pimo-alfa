/**
 * 3d/transforms/drawerTransforms.ts — transforms clássicos (sem europeu).
 */
import type { DrawerLayerItem } from "../../models/BoxLayers";

export function buildDrawerTransforms(_item: DrawerLayerItem): null {
  return null;
}

export const drawerTransforms = {
  build: buildDrawerTransforms,
};

export default drawerTransforms;
