export function useProjectActions() {
// Hook para ações de UI do projeto.
// Expõe funções para adicionar/remover/duplicar caixa, aplicar material, etc.

  // Placeholder: migrar ações reais do ProjectProvider
  return {
    addBox: () => {},
    removeBox: () => {},
    duplicateBox: () => {},
    applyMaterial: () => {},
    // ...outras ações
  };
}
