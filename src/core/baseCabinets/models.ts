import { BOX_MODELS_REGISTRY } from "../../data/moveisUnificados/boxModelsRegistry";
import { PI_BASE_MODELS } from "../../data/moveisUnificados/pi/models";

/**
 * Modelos base de armários (Base Cabinets).
 * Fonte unificada em src/data/moveisUnificados/boxModelsRegistry.ts.
 */
export const BASE_CABINET_MODELS = [...BOX_MODELS_REGISTRY, ...PI_BASE_MODELS];
