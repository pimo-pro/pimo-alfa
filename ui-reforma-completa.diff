diff --git a/src/components/ui/Button.tsx b/src/components/ui/Button.tsx
new file mode 100644
index 0000000..f728420
--- /dev/null
+++ b/src/components/ui/Button.tsx
@@ -0,0 +1,30 @@
+import type { ButtonHTMLAttributes, ReactNode } from "react";
+import "./ui.css";
+
+type ButtonVariant = "primary" | "secondary" | "outline" | "danger" | "ghost" | "link";
+
+type Props = {
+  children: ReactNode;
+  variant?: ButtonVariant;
+  fullWidth?: boolean;
+} & ButtonHTMLAttributes<HTMLButtonElement>;
+
+export default function Button({
+  children,
+  variant = "secondary",
+  className,
+  fullWidth = false,
+  ...props
+}: Props) {
+  return (
+    <button
+      type="button"
+      {...props}
+      className={`ui-button ui-button--${variant}${fullWidth ? " ui-button--full" : ""}${
+        className ? ` ${className}` : ""
+      }`}
+    >
+      {children}
+    </button>
+  );
+}
diff --git a/src/components/ui/Input.tsx b/src/components/ui/Input.tsx
new file mode 100644
index 0000000..1ab5e14
--- /dev/null
+++ b/src/components/ui/Input.tsx
@@ -0,0 +1,23 @@
+import type { InputHTMLAttributes } from "react";
+import "./ui.css";
+
+type Props = {
+  label?: string;
+  error?: string | null;
+  success?: string | null;
+} & InputHTMLAttributes<HTMLInputElement>;
+
+export default function Input({ label, error, success, className, ...props }: Props) {
+  const stateClass = error ? " ui-input--error" : success ? " ui-input--success" : "";
+  return (
+    <label className="ui-form-group">
+      {label ? <span className="ui-input__label">{label}</span> : null}
+      <input
+        {...props}
+        className={`ui-input${stateClass}${className ? ` ${className}` : ""}`}
+      />
+      {error ? <span className="ui-input__error">{error}</span> : null}
+      {!error && success ? <span className="ui-input__success">{success}</span> : null}
+    </label>
+  );
+}
diff --git a/src/components/ui/Card.tsx b/src/components/ui/Card.tsx
new file mode 100644
index 0000000..e8e1c9d
--- /dev/null
+++ b/src/components/ui/Card.tsx
@@ -0,0 +1,21 @@
+import type { ReactNode } from "react";
+import "./ui.css";
+
+type Props = {
+  title?: string;
+  subtitle?: string;
+  children: ReactNode;
+  maxWidth?: number;
+  className?: string;
+};
+
+export default function Card({ title, subtitle, children, maxWidth, className }: Props) {
+  const maxClass = maxWidth === 420 ? " ui-card--max-420" : maxWidth === 640 ? " ui-card--max-640" : "";
+  return (
+    <section className={`ui-card${maxClass}${className ? ` ${className}` : ""}`}>
+      {title ? <h2 className="ui-card__title">{title}</h2> : null}
+      {subtitle ? <p className="ui-card__subtitle">{subtitle}</p> : null}
+      {children}
+    </section>
+  );
+}
diff --git a/src/theme/theme.ts b/src/theme/theme.ts
new file mode 100644
index 0000000..fc22ef1
--- /dev/null
+++ b/src/theme/theme.ts
@@ -0,0 +1,170 @@
+export const theme = {
+  colors: {
+    background: "var(--navy)",
+    surface: "var(--card-bg)",
+    surfaceBorder: "var(--card-border)",
+    text: "var(--text-main)",
+    textMuted: "var(--text-muted)",
+    border: "var(--border)",
+    primary: "var(--blue-light)",
+    primaryText: "#ffffff",
+    secondary: "var(--button-ghost-bg)",
+    secondaryBorder: "var(--button-ghost-border)",
+    secondaryText: "var(--text-main)",
+    outlineText: "var(--text-main)",
+    inputBg: "var(--input-bg)",
+    inputBorder: "var(--input-border)",
+    danger: "#dc2626",
+    success: "#16a34a",
+    focusRing: "var(--focus-ring)",
+  },
+  spacing: {
+    s1: 4,
+    s2: 8,
+    s3: 12,
+    s4: 16,
+    s5: 24,
+    s6: 32,
+    s7: 40,
+    s8: 48,
+  },
+  radius: {
+    sm: "8px",
+    md: "12px",
+    lg: "16px",
+    xl: "20px",
+    pill: "999px",
+  },
+  typography: {
+    fontFamily: 'system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif',
+    h1: { fontSize: 24, fontWeight: 700, lineHeight: 1.2 },
+    h2: { fontSize: 16, fontWeight: 700, lineHeight: 1.3 },
+    body: { fontSize: 14, fontWeight: 400, lineHeight: 1.5 },
+    small: { fontSize: 12, fontWeight: 400, lineHeight: 1.4 },
+    label: { fontSize: 12, fontWeight: 600, lineHeight: 1.4 },
+  },
+  shadows: {
+    xs: "0 1px 2px rgba(0, 0, 0, 0.10)",
+    sm: "0 6px 16px rgba(0, 0, 0, 0.14)",
+    md: "0 10px 28px rgba(0, 0, 0, 0.18)",
+    card: "0 8px 22px rgba(0, 0, 0, 0.14)",
+    none: "none",
+  },
+  components: {
+    button: {
+      minHeight: 42,
+      paddingX: 16,
+      paddingY: 10,
+      borderWidth: 1,
+      primaryBg: "var(--blue-light)",
+      primaryBorder: "var(--blue-light)",
+      secondaryBg: "var(--button-ghost-bg)",
+      secondaryBorder: "var(--button-ghost-border)",
+      secondaryHover: "var(--button-ghost-hover)",
+      disabledOpacity: 0.6,
+    },
+    input: {
+      minHeight: 44,
+      paddingX: 12,
+      paddingY: 10,
+      borderWidth: 1,
+    },
+    card: {
+      borderWidth: 1,
+      padding: 24,
+    },
+  },
+} as const;
+
+export type AppTheme = typeof theme;
+
+export function applyThemeVariables(): void {
+  const root = document.documentElement;
+  const vars: Record<string, string | number> = {
+    "--ui-color-background": theme.colors.background,
+    "--ui-color-surface": theme.colors.surface,
+    "--ui-color-surface-border": theme.colors.surfaceBorder,
+    "--ui-color-text": theme.colors.text,
+    "--ui-color-text-muted": theme.colors.textMuted,
+    "--ui-color-border": theme.colors.border,
+    "--ui-color-primary": theme.colors.primary,
+    "--ui-color-primary-text": theme.colors.primaryText,
+    "--ui-color-input-bg": theme.colors.inputBg,
+    "--ui-color-input-border": theme.colors.inputBorder,
+    "--ui-color-danger": theme.colors.danger,
+    "--ui-color-focus-ring": theme.colors.focusRing,
+
+    "--ui-space-1": theme.spacing.s1,
+    "--ui-space-2": theme.spacing.s2,
+    "--ui-space-3": theme.spacing.s3,
+    "--ui-space-4": theme.spacing.s4,
+    "--ui-space-5": theme.spacing.s5,
+    "--ui-space-6": theme.spacing.s6,
+    "--ui-space-7": theme.spacing.s7,
+    "--ui-space-8": theme.spacing.s8,
+    "--ui-space-xxs": theme.spacing.s1,
+    "--ui-space-xs": theme.spacing.s2,
+    "--ui-space-sm": theme.spacing.s3,
+    "--ui-space-md": theme.spacing.s4,
+    "--ui-space-lg": theme.spacing.s5,
+    "--ui-space-xl": theme.spacing.s6,
+
+    "--ui-radius-sm": theme.radius.sm,
+    "--ui-radius-md": theme.radius.md,
+    "--ui-radius-lg": theme.radius.lg,
+    "--ui-radius-xl": theme.radius.xl,
+    "--ui-radius-pill": theme.radius.pill,
+
+    "--ui-font-family": theme.typography.fontFamily,
+    "--ui-font-size-h1": `${theme.typography.h1.fontSize}px`,
+    "--ui-font-size-h2": `${theme.typography.h2.fontSize}px`,
+    "--ui-font-size-body": `${theme.typography.body.fontSize}px`,
+    "--ui-font-size-small": `${theme.typography.small.fontSize}px`,
+    "--ui-font-size-label": `${theme.typography.label.fontSize}px`,
+    "--ui-font-weight-h1": theme.typography.h1.fontWeight,
+    "--ui-font-weight-h2": theme.typography.h2.fontWeight,
+    "--ui-font-weight-label": theme.typography.label.fontWeight,
+    "--ui-line-height-h1": String(theme.typography.h1.lineHeight),
+    "--ui-line-height-h2": String(theme.typography.h2.lineHeight),
+    "--ui-line-height-body": String(theme.typography.body.lineHeight),
+    "--ui-line-height-small": String(theme.typography.small.lineHeight),
+    "--ui-line-height-label": String(theme.typography.label.lineHeight),
+
+    "--ui-color-secondary": theme.colors.secondary,
+    "--ui-color-secondary-border": theme.colors.secondaryBorder,
+    "--ui-color-secondary-text": theme.colors.secondaryText,
+    "--ui-color-outline-text": theme.colors.outlineText,
+    "--ui-color-success": theme.colors.success,
+
+    "--ui-shadow-card": theme.shadows.card,
+    "--ui-shadow-xs": theme.shadows.xs,
+    "--ui-shadow-sm": theme.shadows.sm,
+    "--ui-shadow-md": theme.shadows.md,
+    "--ui-shadow-subtle": theme.shadows.xs,
+
+    "--ui-button-min-height": theme.components.button.minHeight,
+    "--ui-button-padding-x": `${theme.components.button.paddingX}px`,
+    "--ui-button-padding-y": `${theme.components.button.paddingY}px`,
+    "--ui-button-border-width": theme.components.button.borderWidth,
+    "--ui-button-primary-bg": theme.components.button.primaryBg,
+    "--ui-button-primary-border": theme.components.button.primaryBorder,
+    "--ui-button-secondary-bg": theme.components.button.secondaryBg,
+    "--ui-button-secondary-border": theme.components.button.secondaryBorder,
+    "--ui-button-secondary-hover": theme.components.button.secondaryHover,
+    "--ui-button-ghost-bg": theme.components.button.secondaryBg,
+    "--ui-button-ghost-border": theme.components.button.secondaryBorder,
+    "--ui-button-ghost-hover": theme.components.button.secondaryHover,
+    "--ui-button-disabled-opacity": theme.components.button.disabledOpacity,
+
+    "--ui-input-min-height": theme.components.input.minHeight,
+    "--ui-input-padding-x": `${theme.components.input.paddingX}px`,
+    "--ui-input-padding-y": `${theme.components.input.paddingY}px`,
+    "--ui-input-border-width": theme.components.input.borderWidth,
+
+    "--ui-card-padding": `${theme.components.card.padding}px`,
+  };
+
+  Object.entries(vars).forEach(([key, value]) => {
+    root.style.setProperty(key, String(value));
+  });
+}
diff --git a/src/pages/LoginPage.tsx b/src/pages/LoginPage.tsx
new file mode 100644
index 0000000..bfab2b0
--- /dev/null
+++ b/src/pages/LoginPage.tsx
@@ -0,0 +1,104 @@
+import { useState } from "react";
+import { Link, Navigate, useNavigate } from "react-router-dom";
+
+import { useAuth } from "../auth/useAuth";
+import Button from "../components/ui/Button";
+import Card from "../components/ui/Card";
+import FormGroup from "../components/ui/FormGroup";
+import Input from "../components/ui/Input";
+import PageContainer from "../components/ui/PageContainer";
+import Section from "../components/ui/Section";
+import SocialButtons from "../components/ui/SocialButtons";
+import Tabs from "../components/ui/Tabs";
+import "../components/ui/ui.css";
+
+export default function LoginPage() {
+  const navigate = useNavigate();
+  const { login, isAuthenticated } = useAuth();
+  const [email, setEmail] = useState("admin@pimo.local");
+  const [password, setPassword] = useState("admin123");
+  const [error, setError] = useState<string | null>(null);
+  const [submitting, setSubmitting] = useState(false);
+
+  if (isAuthenticated()) {
+    return <Navigate to="/dashboard" replace />;
+  }
+
+  const handleSubmit = async (event: React.FormEvent<HTMLFormElement>) => {
+    event.preventDefault();
+    setError(null);
+    setSubmitting(true);
+    try {
+      await login(email, password);
+      navigate("/dashboard", { replace: true });
+    } catch (err) {
+      setError(err instanceof Error ? err.message : "Falha no login");
+    } finally {
+      setSubmitting(false);
+    }
+  };
+
+  return (
+    <PageContainer>
+      <div className="ui-auth-page">
+        <Card className="ui-card--max-640">
+          <div className="ui-auth-card">
+            <Tabs
+              activeKey="login"
+              items={[
+                { key: "login", label: "Login", to: "/login" },
+                { key: "register", label: "Registrar", to: "/register" },
+                { key: "forgot", label: "Recuperar senha", to: "/forgot-password" },
+              ]}
+            />
+
+            <Section title="Entrar com parceiros">
+              <SocialButtons
+                items={[
+                  { key: "google", label: "Google" },
+                  { key: "facebook", label: "Facebook" },
+                ]}
+              />
+            </Section>
+
+            <Section title="Acesso por email e senha">
+              <Card subtitle="Preencha seus dados para entrar na plataforma.">
+                <form onSubmit={handleSubmit} className="ui-form-group">
+                  <FormGroup>
+                    <Input
+                      type="email"
+                      label="Email"
+                      value={email}
+                      onChange={(e) => setEmail(e.target.value)}
+                      required
+                    />
+                  </FormGroup>
+                  <FormGroup>
+                    <Input
+                      type="password"
+                      label="Password"
+                      value={password}
+                      onChange={(e) => setPassword(e.target.value)}
+                      required
+                    />
+                  </FormGroup>
+                  <div className="ui-form-group ui-auth-divider">
+                    {error ? <p className="ui-text-danger">{error}</p> : null}
+                    <Button type="submit" variant="primary" disabled={submitting}>
+                      {submitting ? "Entrando..." : "Entrar"}
+                    </Button>
+                    <FormGroup className="ui-auth-divider">
+                      <Link to="/forgot-password" className="ui-link ui-link--primary">
+                        Esqueci minha senha
+                      </Link>
+                    </FormGroup>
+                  </div>
+                </form>
+              </Card>
+            </Section>
+          </div>
+        </Card>
+      </div>
+    </PageContainer>
+  );
+}
